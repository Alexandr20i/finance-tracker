linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused

issues:
  exclude-rules:
    - path: "_test.go"
      linters:
        - errcheck
    - path: "gen/"
      linters:
        - all  # игнорируем сгенерированный код